{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(pnpm type-check:*)",
      "Bash(pnpm lint:*)",
      "Bash(pnpm build:*)",
      "Bash(pnpm dev:*)",
      "Bash(pnpm install:*)",
      "Bash(npx eslint:*)",
      "Bash(npx vue-tsc:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(openspec:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // empty'); if echo \"$FILE\" | grep -qE '\\.(vue|ts|tsx)$' && [ -f \"$FILE\" ]; then OUTPUT=$(cd /Users/haltech/Documents/ERP/hal-erp && npx eslint --fix \"$FILE\" 2>&1); if [ $? -ne 0 ]; then echo \"⚠️  ESLint issues in $FILE:\"; echo \"$OUTPUT\" | tail -20; fi; fi"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/haltech/Documents/ERP/hal-erp && CHANGED=$(git status --short 2>/dev/null | wc -l | tr -d ' '); BRANCH=$(git branch --show-current 2>/dev/null); echo \"📁 hal-erp | branch: $BRANCH | uncommitted files: $CHANGED\""
          }
        ]
      }
    ]
  }
}
